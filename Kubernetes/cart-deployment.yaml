apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart
  namespace: nats-shop
  labels:
    app: cart
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart
  template:
    metadata:
      labels:
        app: cart
    spec:
      containers:
        - name: cart
          image: nats-shop-cart:latest # Placeholder image name
          imagePullPolicy: IfNotPresent
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: NATS_URL
              value: "nats://nats:4222"
            - name: REDIS_CONNECTION_STRING
              value: "valkey:6379"
          resources:
            limits:
              cpu: "0.5"
              memory: "512Mi"
            requests:
              cpu: "0.1"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readinessz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 5
